# Generated by uniffi-bindgen-react-native
cmake_minimum_required(VERSION 3.9.0)
project(ReactNativeRussh)

set (CMAKE_VERBOSE_MAKEFILE ON)
set (CMAKE_CXX_STANDARD 20)

if(DEFINED REACT_NATIVE_DIR)
  set(REACT_NATIVE_CMAKE_UTILS "${REACT_NATIVE_DIR}/ReactAndroid/cmake-utils")
else()
  set(REACT_NATIVE_CMAKE_UTILS "${CMAKE_CURRENT_LIST_DIR}/../../../node_modules/react-native/ReactAndroid/cmake-utils")
endif()
include("${REACT_NATIVE_CMAKE_UTILS}/folly-flags.cmake")
add_compile_options(${folly_FLAGS})

# Resolve the path to the uniffi-bindgen-react-native package
execute_process(
    COMMAND node -p "require('path').resolve(require('path').dirname(require.resolve('uniffi-bindgen-react-native')), '../../..')"
    OUTPUT_VARIABLE UNIFFI_BINDGEN_PATH
    OUTPUT_STRIP_TRAILING_WHITESPACE
)

# Specifies a path to native header files.
include_directories(
    ../cpp
    ../cpp/generated
    build/generated/source/codegen/jni

    ${UNIFFI_BINDGEN_PATH}/cpp/includes
)

# React Native's Android autolinking expects the CMake target to use the
# generated library name. Keep the shared-library filename stable because the
# Kotlin bridge loads it explicitly.
add_library(react_codegen_ReactNativeRusshSpec       SHARED
    ../cpp/react-native-russh.cpp
    ../cpp/generated/react_native_russh.cpp
    cpp-adapter.cpp
    build/generated/source/codegen/jni/ReactNativeRusshSpec-generated.cpp
)
set_target_properties(react_codegen_ReactNativeRusshSpec PROPERTIES
  OUTPUT_NAME "react-native-russh"
)
target_include_directories(react_codegen_ReactNativeRusshSpec PUBLIC
  ${CMAKE_CURRENT_LIST_DIR}/build/generated/source/codegen/jni
  ${CMAKE_CURRENT_LIST_DIR}/build/generated/source/codegen/jni/react/renderer/components/ReactNativeRusshSpec
)
if(COMMAND target_compile_reactnative_options)
  target_compile_reactnative_options(react_codegen_ReactNativeRusshSpec PUBLIC)
endif()

# Set C++ compiler flags
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O2")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fexceptions")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -frtti")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -fstack-protector-all")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Werror")

# Set linker flags for 16KB page size alignment (required for Android 15+)
set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -Wl,-z,max-page-size=16384")

cmake_path(
  SET MY_RUST_LIB
  ${CMAKE_CURRENT_LIST_DIR}/src/main/jniLibs/${ANDROID_ABI}/libreact_native_russh.a
  NORMALIZE
)
add_library(my_rust_lib STATIC IMPORTED)
set_target_properties(my_rust_lib PROPERTIES IMPORTED_LOCATION ${MY_RUST_LIB})

# Add ReactAndroid libraries, being careful to account for different versions.
find_package(ReactAndroid REQUIRED CONFIG)
find_library(LOGCAT log)
find_library(ANDROID_LIB android)

# REACTNATIVE_MERGED_SO seems to be only be set in a build.gradle.kt file,
# which we don't use. Thus falling back to version number sniffing.
if (ReactAndroid_VERSION_MINOR GREATER_EQUAL 76)
  set(REACTNATIVE_MERGED_SO true)
endif()

# https://github.com/react-native-community/discussions-and-proposals/discussions/816
# This if-then-else can be removed once this library does not support version below 0.76
if (REACTNATIVE_MERGED_SO)
  target_link_libraries(react_codegen_ReactNativeRusshSpec ReactAndroid::reactnative)
else()
  target_link_libraries(react_codegen_ReactNativeRusshSpec
    ReactAndroid::turbomodulejsijni
    ReactAndroid::react_nativemodule_core
  )
endif()

find_package(fbjni REQUIRED CONFIG)
target_link_libraries(
  react_codegen_ReactNativeRusshSpec
  fbjni::fbjni
  ReactAndroid::jsi
  ${LOGCAT}
  ${ANDROID_LIB}
  my_rust_lib
)
