<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- AsyncStorage contains host metadata and only AES-GCM-encrypted credential backups. -->
    <include domain="database" path="." />
    <!-- Only user-selected terminal backgrounds are backed up from app files. -->
    <include domain="file" path="terminal-backgrounds/" />
</full-backup-content>
