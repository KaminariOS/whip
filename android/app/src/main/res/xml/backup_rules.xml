<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- AsyncStorage's database contains known SSH hosts, host metadata, bounded diagnostics, and encrypted credential backups. -->
    <include domain="database" path="." />
    <!-- Only user-selected terminal backgrounds are backed up from app files. -->
    <include domain="file" path="terminal-backgrounds/" />
</full-backup-content>
